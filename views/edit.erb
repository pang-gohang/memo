<!DOCTYPE html>
<html lang="ja">
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" type="text/css" href="/semantic.min.css">
    <script src="/jquery-3.7.1.min.js">
    </script>
    <script src="/semantic.min.js">
    </script>
    <title>編集</title>
  </head>
  <body>
    <div class="ui container">
      <div></div>
      <div class="ui segment">
        <h1 class="ui right floated header"><a href="/">しなとらメモアプリ</a></h1>
        <div class="ui clearing divider"></div>
        <div class="ui form">
          <form action="/<%= @memo_id %>" method="post">
            <input type="hidden" name="_method" value="PATCH">
            <div class="field">
              <label>タイトル</label>
              <textarea id="subject" name="subject" required rows="2"><%= @subject %></textarea>
            </div>
            <div class="field">
              <label>内容</label>
              <textarea id="content" name="content" required><%= @content %></textarea>
            </div>
            <td>
              <button input type="submit" value="追加" class="ui black button">
                登録
              </button>
            <button class="ui black basic button" onclick="location.href = '/'">戻る</button>
            </td>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
