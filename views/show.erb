<div class="ui container">
  <div></div>
  <div class="ui segment">
    <h1 class="ui right floated header"><a href="/">しなとらメモアプリ</a></h1>
    <div class="ui clearing divider"></div>
    <div class="ui message"><h2><%= h @subject %></h2></div>
    <div class="ui message"><%= h @content %></div>
    <button class="ui black button" onclick="location.href = '/memos/<%= @memo_id %>/edit'">編集</button>
    <button class="ui black basic button" onclick="location.href = '/'">戻る</button>
  </div>
<div>
  <form action="/memos/<%= @memo_id %>" method="post" class="delete-form">
    <input type="hidden" name="_method" value="DELETE">
    <input class="ui black button" type="submit" value="削除">
  </form>
</div>
